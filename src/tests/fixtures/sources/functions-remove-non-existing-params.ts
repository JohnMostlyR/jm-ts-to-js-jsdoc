/*
 * Source file for testing that the program removes tags for non-existing parameters.
 */

/**
 * A function
 * @param {number} notThere
 * @param {string} alsoNotThere - Copy/Paste from somewhere? Stackoverflow? 😁
 */
function aFunction(iDoExist: string): number {
  return 1;
}
