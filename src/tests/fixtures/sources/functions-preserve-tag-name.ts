/*
 * Source file for testing that the program does not modify a tag's name
 */

/**
 * A function
 * @arg {*} param
 */
function aFunction(param: string): number {
  return 1;
}

/**
 * B function
 * @argument {*} param
 */
function bFunction(param: string): number {
  return 1;
}

/**
 * C function
 * @return {*}
 */
function cFunction(param: string): number {
  return 1;
}
