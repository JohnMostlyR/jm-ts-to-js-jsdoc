/*
 * Source file for testing that the program documents a class that implements an
 * interface.
 */

/**
 * @typedef {object} A
 * @property {number} x
 */

/**
 * @implements {A}
 */
class C {
    x = 0;
}
