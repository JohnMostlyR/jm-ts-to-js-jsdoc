/*
 * Source file for testing that the program removes any comments from types.
 */

/**
 * A descriptor for a SplitView instance.
 * @typedef {object} ISplitViewDescriptor
 * @property {number} size The layout size.
 * @property {{ readonly visible?: boolean; readonly size: number; }[]} views Descriptors.
 */
