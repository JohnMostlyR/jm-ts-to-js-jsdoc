/*
 * Source file for testing that the program corrects the type for an existing `@param` tag
 */

/**
 * A function
 * @param {string} [param] - This should be of type string and optional.
 * @returns {number}
 */
function aFunction(param) {
    return 1;
}
