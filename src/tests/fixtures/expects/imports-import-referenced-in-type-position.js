// @ts-nocheck

/*
 * Source file for testing that the program documents a type-only import declaration
 * with named imports when only referenced in a type position.
 */

import { Node } from 'ts-morph';

/**
 * @param {Node} node
 */
function aFunction(node) {
    /* empty */
}
